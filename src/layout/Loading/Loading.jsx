import React from 'react';
import Logo from '../../assets/img/logo.png';

function Loading() {
    return(
        <div className="Loading">
            <img class="logo" src={Logo} alt="Horizons Logo" />
        </div>
    );
};

export default Loading;

/*

            <p>Loading</p>
            */